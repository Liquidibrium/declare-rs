

```shell
# initialize the project
declare init --currency-from <currency> --currency-to <currency> --csv-file <path> --tax <percent>

# add a new transaction
declare --date <date> --amount <amount> --currency-from <currency> --currency-to <currency> --date <date> --exchange-rate <rate>

# show all transactions
declare show  --output <format>

# show currency exchange rate
declare exchange --currency-from <currency> --currency-to <currency> --amount <amount> --date <date>

```

